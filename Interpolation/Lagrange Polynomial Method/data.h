#ifndef DATA_H
#define DATA_H
/**
 * A helper datatype to store (x, y) pairs to represent the value of the
 * given function at the presented points.
 */
typedef struct data_struct {
  double x;
  double y;
} Data;
#endif // DATA_H
